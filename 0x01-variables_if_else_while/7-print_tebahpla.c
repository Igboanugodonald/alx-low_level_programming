#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char _dee = 'z';

	while (_dee >= 'a')
	{
		putchar(_dee);
		_dee--;
	}
		putchar('\n');
		return (0);
}
